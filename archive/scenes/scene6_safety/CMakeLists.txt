cmake_minimum_required(VERSION 3.12)
project(WindAGC_Scene6 VERSION 1.0 LANGUAGES CXX C)
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_C_STANDARD 11)
if(MSVC)
    add_compile_options(/utf-8 /wd4828)
    add_compile_definitions(_CRT_SECURE_NO_WARNINGS)
endif()
set(RTDB_ROOT "C:/Users/32567/Desktop/实验室/风电AGC/储能协调控制器实时数据库与缓存模块详细设计/储能协调控制器实时数据库与缓存模块详细设计")
include_directories(${RTDB_ROOT}/include ${CMAKE_SOURCE_DIR})
add_executable(wind_agc_scene6 main.cpp RtDbLogger.cpp)
target_link_libraries(wind_agc_scene6 "${RTDB_ROOT}/build/Release/rt_db.lib")
set_target_properties(wind_agc_scene6 PROPERTIES RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/bin")
